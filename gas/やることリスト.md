# やることリスト

シート7の週次更新まわりで、まだ片付いていないもの。
片付いたら消す。

## シートの構造

- [ ] **楽天ポイントを亮太／明子の2行に分ける**
  - 現状は66行に合算（`=564019+278002` の数式）。シートの他の項目は
    すべて名義別なので、ここだけ例外になっている
  - 挿入位置は `ポイ投` ブロックの小計の SUM 範囲内（66行の直後）。
    範囲外に足すと小計に入らない
  - 既存66行を `楽天ポイント（亮太）` に改名し、下に `楽天ポイント（明子）`
    を新設するのが素直。ただし66行の過去の値は合算のままなので、
    切り替えた列にメモを残す
  - 済んだら `listRowKeys()` を実行してキーを確認し、MAPPING を書き換える
  - 楽天カード（亮太）の Visa／Mastercard は**分けない**。同一人物で
    Visa は常に ¥0。数式 `=0+531222` で内訳が見えれば足りる

- [ ] **CG列（8/5）にメモを残す** 「ここから合計基準（未確定込み）」。
  負債が 2,152,030 → 4,525,177 に跳ねた理由の記録

- [ ] 未連携カードの扱いを決める。BICカメラSuica / ニッセンマジカル /
  三菱UFJ / JOSHIN / Tカードプラス。連携するか、使っていないなら
  シートから行を落とすか

## Moneytree

- [ ] **Amex を再連携する。** 「接続状況を確認してください」が出たままで、
  -¥351,275 は接続が切れた時点の値。せどりで一番動くカードなので
  ズレが大きくなる

- [ ] **明子名義の銀行を連携する。** 三菱UFJ／楽天／横浜銀行
- [ ] **連携時に口座名をシートと同じ表記に変更する。**
  現状は「普通」としか出ず、亮太と明子を区別できない。
  `三菱UFJ（亮太）` `三菱UFJ（明子）` のように全角括弧で揃える。
  既存のみずほ・三菱UFJも同時に付け直す
- [ ] 連携後に銀行セクションのスクショをもらい、MAPPING を確定する

## 取得元が未確認

- [ ] `資産/ポイ投/iPhoneSE12 PayPay` と `iPhoneSE11 PayPay` は何の残高か。
  どの画面から取るか
- [ ] `資産/その他/LINEPay` は今も使っているか。国内サービスは終了している
  はずなので、行ごと落とす選択もある
- [ ] `資産/その他/ポイントサイト` の取得元
- [ ] `資産/金券/アップルギフトカード` の取得元
- [ ] `資産/在庫/商品` の取得元
- [ ] `資産/銀行/タンス` の更新方法

## 運用の決めごと

- [ ] **明子アカウントの扱い。** Yahoo!フリマと楽天ポイントは明子名義の
  別アカウント。毎週ログインし直すのは続かない。
  選択肢: 明子のスマホから送ってもらう／月1回にする／手入力のまま
- [ ] **各カードの正確な締め日。** `payment-reminder.gs` は 15日締め・
  月末締めの暫定値。正確な日付が分かれば `CARD_GROUPS` を差し替える

## エディタへの反映（未実施）

コードは更新済みだが Apps Script 側に貼っていない分。
追加項目が出揃ったタイミングでまとめて貼り替える。

- [ ] `コード.gs` ← `asset-sheet-weekly.gs`（列追加時の文字色リセット）
      貼り替え後に `notifyEmail` を入れ直すこと
- [ ] `import-balances` ← `import-balances.gs`
      （全46行のマッピング、赤字表示、合算の数式化）

## いつかやるかもしれないもの

- [ ] **在庫の計上。** せどりの仕入れをカードで切ると負債だけ増え、
  対応する在庫が資産に載らないため純資産が過小に出る。
  ただし在庫管理はマネーフォワードクラウドで記帳済みなので、
  やるなら月1回、貸借対照表の棚卸資産を1セル転記するだけの形にする
- [ ] **投資信託の評価額を自動計算する。** 口数 × 基準価額。
  結楓・結依は積立が止まっていて口数が変わらないため完全自動化できる。
  亮太・明子は月1回の口数更新が要る。国内投信は `GOOGLEFINANCE` が
  非対応なので基準価額は GAS で取得する
